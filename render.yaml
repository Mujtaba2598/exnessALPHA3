services:
  - type: web
    name: halal-trading-bot
    runtime: node
    plan: free
    buildCommand: npm install && mkdir -p data/trades
    startCommand: node server.js
    healthCheckPath: /api/health
